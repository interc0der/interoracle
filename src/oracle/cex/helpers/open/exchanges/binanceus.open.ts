/**
 * 
 * BINANCEUS: Send request to opened websocket
 * 
 * @param {WebSocket} ws - Exchange socket for sending request
 * @param {string[]} channels - Array of exchange pairs (interoracle format)
 * @returns blank
 */

export const binanceus = (
    ws:WebSocket,
    channels:string[]) => {
    return
};